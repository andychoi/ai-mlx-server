[project]
name = "mlx-server"
version = "0.1.0"
description = "MLX inference server for Apple Silicon — OpenAI-compatible API with embeddings and LoRA support"
requires-python = ">=3.11"
dependencies = [
    "mlx-lm>=0.22",
    "mlx-embeddings>=0.1",
    "psutil",
]

[project.optional-dependencies]
vlm = ["mlx-vlm>=0.2"]
lora = ["datasets", "mlx-lm[lora]"]
metrics = ["prometheus_client"]
image = ["mflux>=0.7", "Pillow>=10"]
dev = ["pytest", "httpx"]

[tool.setuptools]
packages = []
py-modules = ["server", "cache", "invoke", "metrics", "mlx_tool", "imager"]

[project.scripts]
mlx-server = "server:main"
ai-mlx-imager = "imager:main"
mlx-tool = "mlx_tool:main"
